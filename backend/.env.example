# =========================
# OpenAI
# =========================
OPENAI_API_KEY=sk-xxx
OPENAI_MODEL=gpt-4o-mini

# =========================
# Google OAuth
# =========================
# 從 GCP Console > APIs & Services > Credentials 取得
GOOGLE_CLIENT_ID=xxx.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=xxx

# OAuth 回調路徑（預設為 /auth/google/callback）
# redirect_uri = FRONTEND_URL + GOOGLE_OAUTH_CALLBACK_PATH
GOOGLE_OAUTH_CALLBACK_PATH=/auth/google/callback

# [已棄用] 若設定此值，將覆蓋動態產生的 redirect_uri
# GOOGLE_REDIRECT_URI=

# =========================
# JWT
# =========================
# 請使用強隨機字串，可用 openssl rand -hex 32 或 python3 -c "import secrets; print(secrets.token_urlsafe(64))" 產生
JWT_SECRET_KEY=change-this-secret-key-in-production
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# =========================
# Server
# =========================
HOST=0.0.0.0
PORT=8000
ENV=development

# =========================
# CORS
# =========================
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# =========================
# Frontend
# =========================
# 前端 URL（OAuth 回調後重導向用）
FRONTEND_URL=http://localhost:5173
